/** @type {import('next').NextConfig} */
const nextConfig = {
    i18n: {
      locales: ['en', 'en-US', 'ne', 'hi-IN'],
      defaultLocale: 'en',
      localeDetection: false,
    },
  };
  
  export default nextConfig;
  